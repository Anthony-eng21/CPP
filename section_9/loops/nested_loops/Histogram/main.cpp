#include <iostream>
#include <vector>
// INNER LOOP, LOOPS FASTER THAN THE OUTER LOOP
using namespace std;
int main()
{
    int num_items{};

    cout << "How may data items do you have";
    cin >> num_items;

    vector<int> data{};

    for (int i{1}; i <= num_items; ++i)
    {
        
    }

    std::cout << std::endl;
    return 0;
}
